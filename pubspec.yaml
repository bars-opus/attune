name: attune
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 2.0.0+130

environment:
  sdk: ^3.7.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.2
  json_annotation: ^4.9.0
  equatable: ^2.0.5
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.1
  font_awesome_flutter: ^10.8.0
  url_launcher: ^6.3.1
  intl: ^0.19.0
  debounce_throttle: ^2.0.0
  freezed_annotation: ^2.4.1
  image_picker: ^1.0.4
  file_picker: ^6.1.1
  mime: ^1.0.4
  supabase_flutter: ^2.1.0
  google_sign_in: ^7.0.0
  sign_in_with_apple: ^5.0.0
  sentry_flutter: ^8.3.0
  path: ^1.9.0
  glass_kit: ^4.0.2
  image_cropper: ^4.0.0
  flutter_image_compress: ^2.1.0
  path_provider: ^2.1.1
  geolocator: ^11.0.0  
  geocoding: ^4.0.0
  flutter_native_timezone: ^2.0.0
  table_calendar: ^3.0.9
  flutter_secure_storage: ^9.0.0
  sqlite3: ^2.4.6
  sqlite3_flutter_libs: ^0.5.24
  drift: ^2.20.0
  reorderables: ^0.6.0
  uuid: ^3.0.7
  mapbox_maps_flutter: ^2.1.0
  permission_handler: ^11.0.0
  geojson: ^1.0.0
  connectivity_plus: ^4.0.2
  flutter_svg: ^2.0.9
  flutter_animate: ^4.5.0
  vibration: ^1.8.4
  audioplayers: ^6.1.0
  record: ^6.0.0
  video_player: ^2.9.0
  video_thumbnail: ^0.5.3
  video_compress: ^3.1.2
  camera: ^0.11.0
  vector_graphics: ^1.1.10
  vector_graphics_compiler: ^1.1.10
  http: ^0.13.6
  crypto: ^3.0.3
  pointycastle: ^3.9.1
  image: ^4.3.0
  archive: ^3.6.1
  fl_chart: ^0.68.0
  csv: ^6.0.0
  excel: ^4.0.0
  share_plus: ^7.0.0
  qr_flutter: ^4.1.0
  webview_flutter: ^4.4.0
  onesignal_flutter: 5.5.1
  app_links: ^6.0.0
  rename: ^2.0.1
  phone_numbers_parser: ^9.0.23
  countries_utils: ^1.0.1

  flutter_localizations:
    sdk: flutter
  go_router: ^10.1.2
  gap: ^3.0.0
  flutter_screenutil: ^5.9.0
  flutter_slidable: ^4.0.3
  emoji_picker_flutter: ^4.4.0

dependency_overrides:
  web:
    version: 1.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/attune_launcher_icon.png"
  adaptive_icon_background: "#4F9F3A"
  adaptive_icon_foreground: "assets/images/attune_logo_white.png"
  min_sdk_android: 21
  remove_alpha_channel_ios: true
  web:
    generate: false
  windows:
    generate: false
  macos:
    generate: false

flutter_native_splash:
  image: assets/images/attune_logo_white.png
  color: "#4F9F3A"
  android: true
  ios: true
  web: false
  android_12:
    image: assets/images/attune_logo_white.png
    color: "#4F9F3A"


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  build_runner: ^2.4.7
  mocktail: ^1.0.0
  riverpod_generator: ^2.3.0
  freezed: ^2.4.8
  json_serializable: ^6.7.1
  flutter_launcher_icons: ^0.14.1

  # Direct test-time deps: chat_text_field_test.dart fakes these plugins'
  # platform-interface singletons so the press-and-hold voice recording
  # gesture's permission/record calls resolve under `flutter test` instead
  # of throwing MissingPluginException (no real platform channel in a
  # widget-test host). Already present transitively via permission_handler
  # and record; declared directly here only to satisfy
  # depend_on_referenced_packages.
  permission_handler_platform_interface: ^4.3.0
  record_platform_interface: ^1.6.0
  flutter_native_splash: ^2.3.6
  path_provider_platform_interface: ^2.1.2
  plugin_platform_interface: ^2.1.8


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/config/
    - assets/sounds/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
