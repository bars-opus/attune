import 'package:flutter/material.dart';

/// Temporary placeholder for Ask2Flow widget.
/// Real implementation will be added in Task 5.
class Ask2Flow extends StatelessWidget {
  final String relationshipId;

  const Ask2Flow({
    required this.relationshipId,
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox.shrink();
  }
}
