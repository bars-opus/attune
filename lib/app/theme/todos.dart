// lets chache the opinions and forums like the previous 5s, so since its just text ir would really take time, so when the apps reload we shows the caches insteantly before we load the new ones and then refresh the the feed. 
// . The next move is on your side — get me AWS Rekognition credentials (or set them up yourself via supabase secrets set) and a linked Supabase project, and then step 4 is the real test of whether it all works end-to-end.
//Build a fallback web page at /i/<code> for users without the app installed (App Store / Play Store redirect by platform) — no web/ target exists in this repo to build it from.
// add the playstor and app store packeges fro links to work well when app is not installed


// Still outstanding (not blocking)
// Android SHA256 fingerprint in assetlinks.json
// Real App Store / Play Store URLs in i/index.html (currently placeholders)
// paths includes a speculative /invite* that nothing uses — harmless, could trim to just /i/*
// The Attune Flutter repo has uncommitted changes from all of today's work (theme fixes, journal docs, delete dialog, deep link changes) — want me to commit and push those?
