<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Runtime permissions the app actually requests. Without the
         declaration here, Android denies the request outright and the
         permission never appears under App info > Permissions — the same
         symptom the missing NSMicrophoneUsageDescription caused on iOS.

         Only capabilities genuinely used are declared; each is annotated
         with what uses it so an unused one can be removed with confidence
         rather than left "just in case" (every extra permission is
         something the Play listing has to justify). -->

    <!-- record: voice messages in chat (VoiceRecorderService). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- camera: ephemeral photo/video capture (EphemeralCameraScreen), and
         image_picker's fromCamera path. -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- image_picker / file_picker gallery reads. READ_MEDIA_* are the
         Android 13+ (API 33) replacements; READ_EXTERNAL_STORAGE is kept
         with maxSdkVersion so older devices still work without asking for
         broad storage access on new ones. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- geolocator: optional partner location sharing (LocationService).
         COARSE is declared alongside FINE because Android 12+ lets the user
         grant only approximate location, and a FINE-only declaration gives
         them no way to accept that weaker option. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Networking (Supabase, media upload/download, Claude API). -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- onesignal_flutter, which calls OneSignal.Notifications
         .requestPermission() on startup (OneSignalService). Required from
         Android 13 (API 33) onward. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="attune"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Custom scheme (attune://invite?code=..., attune://auth/callback) —
                 opened directly by anything that already has the raw link
                 (Messages, email, third-party QR apps). Does not help the stock
                 iOS/Android Camera QR scanner; see the https intent-filter below
                 for that. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="attune"/>
            </intent-filter>
            <!-- Android App Links — autoVerify lets Android open these
                 https://invite.attune.barsopus.com links directly in the app
                 (skipping the disambiguation dialog) once
                 assetlinks.json is deployed and verified. This is also what
                 the stock Camera app's QR scanner can actually open, unlike
                 the attune:// scheme above. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="invite.attune.barsopus.com"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
